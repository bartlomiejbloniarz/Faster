<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bloniarz.faster">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature android:name="android.software.companion_device_setup"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_faster"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_faster_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Faster">
        <activity android:name=".GiftActivity"/>
        <activity android:name=".GiftsActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ScoreboardActivity" />
        <activity android:name=".GameOverActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameActivity" android:screenOrientation="portrait"/>
    </application>

</manifest>